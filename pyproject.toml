[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wte-trend-viewer"
version = "0.1.0"
description = "Modern SCADA trend viewer desktop application."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "PySide6>=6.8,<7",
    "numpy>=2,<3",
    "polars[calamine]>=1.30,<2",
    "pyqtgraph>=0.13,<0.14",
]

[project.scripts]
wte-trend-viewer = "wte_trend_viewer.app:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
